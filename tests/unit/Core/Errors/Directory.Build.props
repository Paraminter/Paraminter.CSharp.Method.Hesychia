<Project>

    <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />

    <PropertyGroup>
        <RootNamespace>Paraminter.CSharp.Method.Hesychia.Errors</RootNamespace>
    </PropertyGroup>
    
</Project>